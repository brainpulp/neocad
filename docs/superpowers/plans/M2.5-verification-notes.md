# M2.5 Verification Notes

**Date:** 2026-06-18
**Branch:** `m2_5-quick-wins`
**Method:** Vite dev server driven via the browser preview tools (real WebGL).

## Observed behaviors

| Behavior | Result |
|---|---|
| Empty grid shows "Pick a stock from the left to start building." hint; SCENE panel shows "empty" | ✅ |
| Hint disappears after first piece placed | ✅ |
| Scene tree lists every piece and fastener (`Weld: Rod ↔ Block`) | ✅ |
| Clicking a piece row selects it (drives `selectedId` + Properties) | ✅ |
| `Delete` key removes the selected piece **and its fasteners** | ✅ (`parts 2→1`, `fasteners 1→0`) |
| `Ctrl+Z` undoes the delete | ✅ (`parts 1→2`, `fasteners 0→1`) |
| Deleting a fastener via its tree-row ✕ removes only the fastener | ✅ (`fasteners 1→0`, pieces kept) |
| `Esc` cancels the active stock tool | ✅ (palette tool active → inactive) |

## Notes
- Status bar now reads `parts: N · fasteners: N · running/paused` — used as the verification signal.
- Keyboard handler correctly ignores events whose target is a form field (unit-tested via `isEditableTarget`).
- 58 unit/component tests passing.

## Out of scope (→ M-Transform plan)
- Move/rotate/scale gizmo, inline editable dimensions, live-intervene grab.
